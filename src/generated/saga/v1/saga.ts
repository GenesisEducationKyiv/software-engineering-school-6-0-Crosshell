// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: saga/v1/saga.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "saga.v1";

export interface SendConfirmationEmailRequest {
  correlationId: string;
  email: string;
  confirmUrl: string;
  unsubscribeUrl: string;
}

export interface SendConfirmationEmailResponse {
}

function createBaseSendConfirmationEmailRequest(): SendConfirmationEmailRequest {
  return { correlationId: "", email: "", confirmUrl: "", unsubscribeUrl: "" };
}

export const SendConfirmationEmailRequest: MessageFns<SendConfirmationEmailRequest> = {
  encode(message: SendConfirmationEmailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.correlationId !== "") {
      writer.uint32(10).string(message.correlationId);
    }
    if (message.email !== "") {
      writer.uint32(18).string(message.email);
    }
    if (message.confirmUrl !== "") {
      writer.uint32(26).string(message.confirmUrl);
    }
    if (message.unsubscribeUrl !== "") {
      writer.uint32(34).string(message.unsubscribeUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendConfirmationEmailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendConfirmationEmailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.correlationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.confirmUrl = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.unsubscribeUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SendConfirmationEmailRequest>, I>>(base?: I): SendConfirmationEmailRequest {
    return SendConfirmationEmailRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SendConfirmationEmailRequest>, I>>(object: I): SendConfirmationEmailRequest {
    const message = createBaseSendConfirmationEmailRequest();
    message.correlationId = object.correlationId ?? "";
    message.email = object.email ?? "";
    message.confirmUrl = object.confirmUrl ?? "";
    message.unsubscribeUrl = object.unsubscribeUrl ?? "";
    return message;
  },
};

function createBaseSendConfirmationEmailResponse(): SendConfirmationEmailResponse {
  return {};
}

export const SendConfirmationEmailResponse: MessageFns<SendConfirmationEmailResponse> = {
  encode(_: SendConfirmationEmailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendConfirmationEmailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendConfirmationEmailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SendConfirmationEmailResponse>, I>>(base?: I): SendConfirmationEmailResponse {
    return SendConfirmationEmailResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SendConfirmationEmailResponse>, I>>(_: I): SendConfirmationEmailResponse {
    const message = createBaseSendConfirmationEmailResponse();
    return message;
  },
};

/**
 * Server-side implementation lives in the notifier worker (the saga
 * participant that owns the mailer). Client-side implementation lives in the
 * API service's SubscribeSagaOrchestrator, as an alternative to the existing
 * RabbitMQ command/reply transport (see SAGA_MAIL_TRANSPORT).
 */
export type SagaMailServiceService = typeof SagaMailServiceService;
export const SagaMailServiceService = {
  sendConfirmationEmail: {
    path: "/saga.v1.SagaMailService/SendConfirmationEmail" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SendConfirmationEmailRequest): Buffer =>
      Buffer.from(SendConfirmationEmailRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SendConfirmationEmailRequest => SendConfirmationEmailRequest.decode(value),
    responseSerialize: (value: SendConfirmationEmailResponse): Buffer =>
      Buffer.from(SendConfirmationEmailResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SendConfirmationEmailResponse => SendConfirmationEmailResponse.decode(value),
  },
} as const;

export interface SagaMailServiceServer extends UntypedServiceImplementation {
  sendConfirmationEmail: handleUnaryCall<SendConfirmationEmailRequest, SendConfirmationEmailResponse>;
}

export interface SagaMailServiceClient extends Client {
  sendConfirmationEmail(
    request: SendConfirmationEmailRequest,
    callback: (error: ServiceError | null, response: SendConfirmationEmailResponse) => void,
  ): ClientUnaryCall;
  sendConfirmationEmail(
    request: SendConfirmationEmailRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SendConfirmationEmailResponse) => void,
  ): ClientUnaryCall;
  sendConfirmationEmail(
    request: SendConfirmationEmailRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SendConfirmationEmailResponse) => void,
  ): ClientUnaryCall;
}

export const SagaMailServiceClient = makeGenericClientConstructor(
  SagaMailServiceService,
  "saga.v1.SagaMailService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): SagaMailServiceClient;
  service: typeof SagaMailServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
